import {
  BrowserRouter,
  Route,
  RouteComponentProps,
  Switch,
  withRouter,
} from 'react-router-dom';

export { Route, Switch, withRouter, RouteComponentProps };

export const Router = BrowserRouter;
