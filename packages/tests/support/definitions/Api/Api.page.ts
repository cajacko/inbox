import { ICondition } from '../../utils/ensureCondition';

class Api {
  public async data(conditional: ICondition, key: string) {
    throw new Error('No implemented yet');
  }
}

export default new Api();
